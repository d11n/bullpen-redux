/* eslint-disable max-len */
// eslint-disable-next-line max-params
(function main(assert) {
    describe('BULLPEN Redux module exports as expected', describe_export_structure);
    return true;

    // -----------

    function describe_export_structure() {
        return it('Should be true', test_export_structure);
    }
    function test_export_structure() {
        return assert(true);
    }
}(
    require('assert'),
));
